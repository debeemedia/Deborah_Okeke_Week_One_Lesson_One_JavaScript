// Use template literals

const multiline = `template
literals
allow
multiline
strings`
console.log(multiline)

const firstName = 'Deborah'
const lastName = 'Okeke'
const morale = `You're doing well, ${firstName} ${lastName}!`
console.log(morale)
